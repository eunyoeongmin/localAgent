# バグリポート: ngrok接続の不安定性とLLMサーバー接続エラーの解決

## 1. 現象 (Issue)
- **エラーメッセージ:** `openai.APIConnectionError: Connection error.` および `httpcore.ConnectError`.
- **発生状況:** 
  - アプリの起動直後の最初の質問時。
  - しばらく操作しなかった後の再開時。
  - ngrokを通じてローカルLLMサーバー(Lemonade)に接続する際に頻発。

## 2. 原因分析 (Root Cause Analysis)
- **ngrokの休止:** 一定時間リクエストがないと、ngrokトンネルまたは背後のサーバーがアイドル状態になり、最初のパケット処理に時間がかかる。
- **タイムアウ不足:** デフォルトの接続タイムアウト(30秒)では、サーバーの「目覚め」を待つ間にタイムアウトが発生していた。
- **リクエストの並列処理:** 短時間に連続して接続を生成しようとすると、ngrokの無料枠制限(同時接続数)に抵触し、接続が拒否される場合があった。

## 3. 修正内容 (Solutions Applied)

### A. 共有HTTPクライアントの導入 (`config.py`)
- `httpx.AsyncClient`をグローバルに1つだけ生成し、LLM呼び出しごとに使い回すように変更。
- **メリット:** 接続プール(Connection Pool)を維持することで、TCP/TLSハンドシェイクのオーバーヘッドを削減。

### B. タイムアウト設定の大幅緩和 (`config.py`)
- `connect` タイムアウトを **30秒 → 120秒** に変更。
- サーバーが応答を開始するまで十分に待てるように調整。

### C. 強力な再試行(Retry)ロジックの実装 (`app.py`)
- LLM呼び出し部分を `try-except` で囲み、失敗時に **5秒待機して最大3回まで再試行** するループを実装。
- 今回の「最初の質問だけエラーになる」現象を、ユーザーの手を煩わせずに自動で解決。

### D. ngrok専用ヘッダーの付与 (`config.py`)
- `ngrok-skip-browser-warning: true` ヘッダーを固定で付与。
- ngrokの警告ページが挟まることによる解析エラーを完全に防止。

### E. 言語ポリシーの徹底 (`app.py`, `config.py`, `tools/file_ops.py`)
- コード内に混入していた韓国語をすべて日本語に置換。
- `GEMINI.md`に定められた「UI・システムメッセージは日本語のみ」という規則を厳守。

## 4. 今後の推奨事項
- サーバー側のログ(Lemonade Server)を監視し、GPUのメモリ不足による応答遅延が発生していないか定期的に確認。
- 接続エラーが続く場合は、ngrokの固定ドメイン使用や、タイムアウトをさらに調整することを検討。
